/* STEP 1: Basic browser functions
    There are a number of functions included with most modern browsers.
    Try using replace() on a string and logging the result. */

// Note: functions included with your browser are technically called 'methods'

/* STEP 2: Anonymous versus named functions
    Add a click event listener to the #step2 button using an anonymous function, then a named function */
var step2Button = document.querySelector('#step2');

// Anonymous function

// Named function

/* STEP 3: Function parameters
    Some functions require no parameters, some require them, and some have optional ones. */

// No parameters needed - build a function called add() that adds 20 + 30 and logs the result

// Two parameters needed - build a function called addTwo(x, y) that logs x + y

// Parameters optional - try join() with and without a delimiter on an array

// Next, open up scope.html
// This page inspired by and adapted from https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Building_blocks/Functions
