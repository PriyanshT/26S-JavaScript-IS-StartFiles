// STEP 1: Create a reference for the button element

// STEP 2: Create a function called displayMessage() - we will build it out in STEP 3 onwards

// STEP 3: Build an onclick event handler for the button that calls displayMessage()

function displayMessage(msgText, msgType) {
    // STEP 3a: Create a P element referenced by var msgPanel

    // STEP 3b: Add a class attribute of "msgPanel" to it

    // STEP 3c: Insert the message text into the paragraph element

    // STEP 3d: Append the element to the document body

    // STEP 4a: Create a close button element

    // STEP 4b: Add the letter 'X' to the button

    // STEP 4c: Append the close button as a child of msgPanel

    // STEP 5: Add an onclick event to the close button that removes msgPanel from the DOM

    // STEP 6a: Revise displayMessage() to accept a parameter 'msgText'
    // STEP 6b: Update the call at STEP 3 to pass in a message string
    // STEP 6c: Use the msgText parameter in STEP 3c

    // STEP 7a: Add a second parameter 'msgType' to displayMessage()
    /* STEP 7b: Build an if/elseif/else that adds a class of 'error', 'warning', or 'default'
       to msgPanel using classList.add() */
}

// This page inspired by and adapted from https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Building_blocks/Build_your_own_function
