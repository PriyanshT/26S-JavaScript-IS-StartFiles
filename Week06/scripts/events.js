// Declare and initialize variables
var btn = document.querySelector('button');
var pageInput = document.querySelector('input');
var h1 = document.querySelector("h1");

// Build a random() helper function that returns a random number between 0 and the given number

// Build a bgChange() function that sets document.body.style.backgroundColor to a random RGB color

// Event Listeners/Handlers
// btn.onclick = bgChange;

/* STEP 1: Experiment with a variety of different events - comment out the onclick above
and try the following on the appropriate elements:
onfocus / onblur, ondblclick, onmouseover / onmouseout, window.onkeypress / onkeydown / onkeyup */

/* STEP 2: Inline event handlers - don't use!
Comment out the event listener/handler, and add the same event as an attribute in the HTML button */

/* STEP 3: Apply event listeners to the group of A, B, C buttons
Use querySelectorAll and a loop to attach a click event to each button */

/* STEP 4a: addEventListener() and removeEventListener()
After clicking button A, remove the click event from buttons B and C */

/* STEP 4b: Put the bgChange() function inside an anonymous function inside the listener */

/* STEP 4c: Add another event listener, then remove it */

// This page inspired by and adapted from https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Building_blocks/Events
